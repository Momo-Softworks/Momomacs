;;; smartparens.el — Disabled: conflicts with parinfer-rust
;;;
;;; smartparens auto-inserts closing parens which fights with parinfer's
;;; paren management.  Kept as an empty module so momo/load-defaults
;;; doesn't error on a missing file.  To re-enable, restore the use-package
;;; form from git history.

(provide 'smartparens)
;;; smartparens.el ends here
